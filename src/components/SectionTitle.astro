---
const { title, subtitle, centered = false } = Astro.props;
---

<div class={`mb-12 ${centered ? 'text-center' : ''}`}>
  <h2 class="text-3xl md:text-4xl font-bold text-blue-800 mb-4">{title}</h2>
  {subtitle && <p class="text-lg text-gray-600 max-w-3xl">{subtitle}</p>}
</div>
