---
import MainLayout from '../layouts/MainLayout.astro';
import SectionTitle from '../components/SectionTitle.astro';
import Button from '../components/Button.astro';
---

<MainLayout title="Produk">
   Hero Section with Gradient 
  <section class="bg-blue-700 to-white py-12 md:py-15">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center">
        <h1 class="text-3xl md:text-5xl font-extrabold text-white mb-4">Produk Olahan Susu</h1>
        <p class="text-lg md:text-xl text-blue-100">
          Produk berkualitas dari Desa Purwobinangun dengan standar produksi terjamin
        </p>
      </div>
    </div>
  </section>

  <section class="py-8 bg-white">
    <div class="container mx-auto px-4">
      <div class="flex flex-wrap justify-center gap-3" id="filter-container">
        <button 
          class="filter-btn active rounded-full px-6 py-3 font-medium transition-all shadow-md bg-blue-600 text-white hover:bg-blue-700" 
          data-category="all"
        >
          Semua Produk
        </button>
        <button 
          class="filter-btn rounded-full px-6 py-3 font-medium transition-all border border-blue-600 text-blue-600 hover:bg-blue-100" 
          data-category="stik-susu"
        >
          Stik Susu
        </button>
        <button 
          class="filter-btn rounded-full px-6 py-3 font-medium transition-all border border-blue-600 text-blue-600 hover:bg-blue-100" 
          data-category="susu-pasteurisasi"
        >
          Susu Pasteurisasi
        </button>
        <button 
          class="filter-btn rounded-full px-6 py-3 font-medium transition-all border border-blue-600 text-blue-600 hover:bg-blue-100" 
          data-category="pudding"
        >
          Pudding
        </button>
        <button 
          class="filter-btn rounded-full px-6 py-3 font-medium transition-all border border-blue-600 text-blue-600 hover:bg-blue-100" 
          data-category="es-krim"
        >
          Es Krim
        </button>
      </div>
    </div>
  </section>

  <section class="py-12 bg-gray-50">
    <div class="container mx-auto px-4">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6" id="products-grid">
         Products will be dynamically rendered here 
      </div>
    </div>
  </section>

  <!-- Product Detail -->
  <section class="py-16 bg-blue-50">
    <div class="container mx-auto px-4">
      <SectionTitle
        title="Detail Produk"
        subtitle="Informasi lengkap mengenai produk olahan susu Desa Purwobinangun"
        centered={true}
      />
      
      <div class="bg-white rounded-lg shadow-lg overflow-hidden p-6 md:p-8">
        <div class="flex flex-col md:flex-row">
          <div class="md:w-1/2 md:pr-8 mb-6 md:mb-0">
            <div class="bg-blue-50 rounded-lg p-6 mb-6">
              <h3 class="text-2xl font-bold text-blue-800 mb-4">Standar Produksi</h3>
              <p class="text-gray-700 mb-4">
                Semua produk olahan susu Desa Purwobinangun diproduksi dengan standar keamanan pangan yang ketat. Proses produksi dilakukan di fasilitas Factory Sharing yang telah memenuhi standar Good Manufacturing Practice (GMP).
              </p>
              <div class="grid grid-cols-2 gap-4">
                <div class="flex items-center">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-green-600 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <span>Bahan Baku Berkualitas</span>
                </div>
                <div class="flex items-center">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-green-600 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <span>Proses Higienis</span>
                </div>
                <div class="flex items-center">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-green-600 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <span>Kontrol Kualitas</span>
                </div>
                <div class="flex items-center">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-green-600 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <span>Kemasan Aman</span>
                </div>
              </div>
            </div>
            
            <div class="bg-blue-50 rounded-lg p-6">
              <h3 class="text-2xl font-bold text-blue-800 mb-4">Sertifikasi</h3>
              <p class="text-gray-700 mb-4">
                Produk olahan susu Desa Purwobinangun telah memiliki sertifikasi yang menjamin kualitas dan keamanan produk.
              </p>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div class="bg-white rounded-lg p-4 border border-blue-100">
                  <div class="flex items-center mb-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-green-600 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <h4 class="font-semibold">Sertifikat Halal</h4>
                  </div>
                  <p class="text-sm text-gray-600">No. 12345678910</p>
                  <p class="text-sm text-gray-600">Berlaku hingga: 31 Des 2025</p>
                  <Button 
                    text="Lihat Sertifikat" 
                    href="#" 
                    variant="outline"
                    size="sm"
                    class="mt-2"
                  />
                </div>
                <div class="bg-white rounded-lg p-4 border border-blue-100">
                  <div class="flex items-center mb-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-green-600 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <h4 class="font-semibold">PIRT</h4>
                  </div>
                  <p class="text-sm text-gray-600">No. 2023456789</p>
                  <p class="text-sm text-gray-600">Berlaku hingga: 15 Jun 2025</p>
                  <Button 
                    text="Lihat Sertifikat" 
                    href="#" 
                    variant="outline"
                    size="sm"
                    class="mt-2"
                  />
                </div>
              </div>
            </div>
          </div>
          
          <div class="md:w-1/2">
            <div class="bg-blue-50 rounded-lg p-6 mb-6">
              <h3 class="text-2xl font-bold text-blue-800 mb-4">Informasi Penyimpanan</h3>
              <p class="text-gray-700 mb-4">
                Untuk menjaga kualitas produk, berikut adalah panduan penyimpanan yang direkomendasikan:
              </p>
              <ul class="space-y-3">
                <li class="flex items-start">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-blue-600 mr-2 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <span>Simpan di lemari pendingin pada suhu 2-8°C</span>
                </li>
                <li class="flex items-start">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-blue-600 mr-2 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <span>Hindari paparan sinar matahari langsung</span>
                </li>
                <li class="flex items-start">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-blue-600 mr-2 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <span>Konsumsi dalam waktu 3-5 hari setelah kemasan dibuka</span>
                </li>
                <li class="flex items-start">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-blue-600 mr-2 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <span>Perhatikan tanggal kedaluwarsa pada kemasan</span>
                </li>
              </ul>
            </div>
            
            <div class="bg-blue-50 rounded-lg p-6">
              <h3 class="text-2xl font-bold text-blue-800 mb-4">Katalog Produk</h3>
              <p class="text-gray-700 mb-6">
                Dapatkan informasi lengkap mengenai semua produk olahan susu Desa Purwobinangun dalam katalog digital kami.
              </p>
              <div class="flex flex-col sm:flex-row gap-4">
                <Button 
                  text="Unduh Katalog (PDF)" 
                  href="#" 
                  variant="primary"
                  size="md"
                />
                <Button 
                  text="Lihat Semua Produk" 
                  href="#" 
                  variant="secondary"
                  size="md"
                />
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</MainLayout>

<style>
  .carousel-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background-color: rgba(255, 255, 255, 0.5);
    transition: all 0.3s ease;
    cursor: pointer;
  }
  
  .carousel-dot.active {
    background-color: white;
    width: 24px;
    border-radius: 4px;
  }
</style>

<script>
  const productsData = {
    'stik-susu': {
      name: 'Stik Susu',
      variants: [
        {
          id: 'stik-susu-original',
          name: 'Stik Susu Original',
          subtitle: 'Rasa Original',
          price: 'Rp 15.000',
          image: '/Stik_Susu.jpg',
          badge: { text: 'Best Seller', color: 'bg-blue-500' }
        },
        {
          id: 'stik-susu-coklat',
          name: 'Stik Susu Coklat',
          subtitle: 'Rasa Coklat',
          price: 'Rp 15.000',
          image: '/Stik_Susu.png?height=300&width=400',
          badge: null
        },
        {
          id: 'stik-susu-keju',
          name: 'Stik Susu Keju',
          subtitle: 'Rasa Keju',
          price: 'Rp 16.000',
          image: '/Stik_Susu.png?height=300&width=400',
          badge: null
        }
      ]
    },
    'susu-pasteurisasi': {
      name: 'Susu Pasteurisasi',
      variants: [
        {
          id: 'susu-pasteurisasi-original',
          name: 'Susu Pasteurisasi',
          subtitle: 'Rasa Original',
          price: 'Rp 7.000',
          image: '/susu_pasteurisasiq.jpg',
          badge: { text: 'Baru', color: 'bg-green-500' }
        },
        {
          id: 'susu-pasteurisasi-coklat',
          name: 'Susu Pasteurisasi',
          subtitle: 'Rasa Coklat',
          price: 'Rp 7.500',
          image: '/susucoklat.png?height=300&width=400',
          badge: null
        },
        {
          id: 'susu-pasteurisasi-stroberi',
          name: 'Susu Pasteurisasi',
          subtitle: 'Rasa Melon',
          price: 'Rp 7.500',
          image: '/susumelon.png?height=300&width=400',
          badge: null
        }
      ]
    },
    'pudding': {
      name: 'Pudding Susu',
      variants: [
        {
          id: 'pudding-vanilla',
          name: 'Pudding Susu',
          subtitle: 'Rasa Vanilla',
          price: 'Rp 8.000',
          image: '/puddingg.png?height=300&width=400',
          badge: null
        },
        {
          id: 'pudding-coklat',
          name: 'Pudding Susu',
          subtitle: 'Rasa Coklat',
          price: 'Rp 8.000',
          image: '/puding.svg?height=300&width=400',
          badge: null
        },
        {
          id: 'pudding-stroberi',
          name: 'Pudding Susu',
          subtitle: 'Rasa Stroberi',
          price: 'Rp 8.000',
          image: '/puddingg.svg?height=300&width=400',
          badge: null
        }
      ]
    },
    'es-krim': {
      name: 'Es Krim Susu',
      variants: [
        {
          id: 'es-krim-vanilla',
          name: 'Es Krim Susu',
          subtitle: 'Rasa Vanilla',
          price: 'Rp 10.000',
          image: '/eskrim1.png?height=300&width=400',
          badge: { text: 'Populer', color: 'bg-orange-500' }
        },
        {
          id: 'es-krim-coklat',
          name: 'Es Krim Susu',
          subtitle: 'Rasa Coklat',
          price: 'Rp 10.000',
          image: '/eskrim2.png?height=300&width=400',
          badge: null
        },//dde
        {
          id: 'es-krim-stroberi',
          name: 'Es Krim Susu',
          subtitle: 'Rasa Stroberi',
          price: 'Rp 10.000',
          image: '/eskrim3.png?height=300&width=400',
          badge: null
        }
      ]
    }
  };

  const carouselStates = {};
  const carouselIntervals = {};

  function createCarouselCard(category, data) {
    const variants = data.variants;
    const carouselId = `carousel-${category}`;
    
    // Initialize carousel state
    carouselStates[carouselId] = 0;

    const card = document.createElement('div');
    card.className = 'product-card bg-white rounded-2xl shadow-md overflow-hidden hover:shadow-lg hover:bg-gray-50 transition-all';
    card.dataset.category = category;
    
    card.innerHTML = `
      <div class="relative h-56 overflow-hidden bg-gray-100">
        <div class="carousel-images relative w-full h-full" data-carousel="${carouselId}">
          ${variants.map((variant, index) => `
            <img 
              src="${variant.image}" 
              alt="${variant.name}" 
              class="carousel-image absolute inset-0 w-full h-full object-contain transition-opacity duration-500 ${index === 0 ? 'opacity-100' : 'opacity-0'}"
              data-index="${index}"
            />
          `).join('')}
        </div>
        ${variants[0].badge ? `
          <div class="absolute top-3 right-3 ${variants[0].badge.color} text-white px-3 py-1 rounded-full text-xs font-bold">
            ${variants[0].badge.text}
          </div>
        ` : ''}
        <div class="absolute bottom-3 left-0 right-0 flex justify-center gap-2">
          ${variants.map((_, index) => `
            <div class="carousel-dot ${index === 0 ? 'active' : ''}" data-carousel="${carouselId}" data-index="${index}"></div>
          `).join('')}
        </div>
      </div>
      <div class="p-5">
        <h3 class="text-lg font-semibold text-gray-800 mb-1">${data.name}</h3>
        <p class="text-sm text-gray-500 italic mb-3">Multiple Variants Available</p>
        <p class="text-blue-600 font-bold text-base mb-4">Mulai dari ${variants[0].price}</p>
        <a 
          href="/produk/${variants[0].id}" 
          class="inline-block w-full text-center rounded-full px-6 py-3 bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors"
        >
          Lihat Detail
        </a>
      </div>
    `;
    
    return card;
  }

  function createVariantCard(variant, category) {
    const card = document.createElement('div');
    card.className = 'product-card bg-white rounded-2xl shadow-md overflow-hidden hover:shadow-lg hover:bg-gray-50 transition-all';
    card.dataset.category = category;
    
    card.innerHTML = `
      <div class="relative h-56 overflow-hidden bg-gray-100">
        <img 
          src="${variant.image}" 
          alt="${variant.name}" 
          class="w-full h-full object-contain"
        />
        ${variant.badge ? `
          <div class="absolute top-3 right-3 ${variant.badge.color} text-white px-3 py-1 rounded-full text-xs font-bold">
            ${variant.badge.text}
          </div>
        ` : ''}
      </div>
      <div class="p-5">
        <h3 class="text-lg font-semibold text-gray-800 mb-1">${variant.name}</h3>
        <p class="text-sm text-gray-500 mb-3">${variant.subtitle}</p>
        <p class="text-blue-600 font-bold text-base mb-4">${variant.price}</p>
        <a 
          href="/produk/${variant.id}" 
          class="inline-block w-full text-center rounded-full px-6 py-3 bg-blue-600 text-white font-medium hover:bg-blue-700 transition-colors"
        >
          Lihat Detail
        </a>
      </div>
    `;
    
    return card;
  }

  function startCarousel(carouselId) {
    const carousel = document.querySelector(`[data-carousel="${carouselId}"]`);
    if (!carousel) return;

    const images = carousel.querySelectorAll('.carousel-image');
    const dots = document.querySelectorAll(`.carousel-dot[data-carousel="${carouselId}"]`);
    
    // Clear existing interval
    if (carouselIntervals[carouselId]) {
      clearInterval(carouselIntervals[carouselId]);
    }

    carouselIntervals[carouselId] = setInterval(() => {
      const currentIndex = carouselStates[carouselId];
      const nextIndex = (currentIndex + 1) % images.length;
      
      // Update images
      images[currentIndex].classList.remove('opacity-100');
      images[currentIndex].classList.add('opacity-0');
      images[nextIndex].classList.remove('opacity-0');
      images[nextIndex].classList.add('opacity-100');
      
      // Update dots
      dots[currentIndex].classList.remove('active');
      dots[nextIndex].classList.add('active');
      
      carouselStates[carouselId] = nextIndex;
    }, 3500); // 3.5 seconds
  }

  function stopAllCarousels() {
    Object.keys(carouselIntervals).forEach(carouselId => {
      if (carouselIntervals[carouselId]) {
        clearInterval(carouselIntervals[carouselId]);
        delete carouselIntervals[carouselId];
      }
    });
  }

  function renderProducts(category) {
    const grid = document.getElementById('products-grid');
    if (!grid) return;

    // Stop all existing carousels
    stopAllCarousels();
    
    // Clear grid
    grid.innerHTML = '';

    if (category === 'all') {
      // Show one carousel card per category
      Object.keys(productsData).forEach(cat => {
        const card = createCarouselCard(cat, productsData[cat]);
        grid.appendChild(card);
      });

      // Start all carousels
      Object.keys(productsData).forEach(cat => {
        const carouselId = `carousel-${cat}`;
        startCarousel(carouselId);
      });

      // Add click handlers for carousel dots
      document.querySelectorAll('.carousel-dot').forEach(dot => {
        dot.addEventListener('click', (e) => {
          const carouselId = e.target.dataset.carousel;
          const targetIndex = parseInt(e.target.dataset.index);
          const carousel = document.querySelector(`[data-carousel="${carouselId}"]`);
          const images = carousel.querySelectorAll('.carousel-image');
          const dots = document.querySelectorAll(`.carousel-dot[data-carousel="${carouselId}"]`);
          
          // Update images
          images.forEach((img, idx) => {
            if (idx === targetIndex) {
              img.classList.remove('opacity-0');
              img.classList.add('opacity-100');
            } else {
              img.classList.remove('opacity-100');
              img.classList.add('opacity-0');
            }
          });
          
          // Update dots
          dots.forEach((d, idx) => {
            if (idx === targetIndex) {
              d.classList.add('active');
            } else {
              d.classList.remove('active');
            }
          });
          
          carouselStates[carouselId] = targetIndex;
          
          // Restart carousel
          startCarousel(carouselId);
        });
      });
    } else {
      // Show all variants for selected category
      const categoryData = productsData[category];
      if (categoryData) {
        categoryData.variants.forEach(variant => {
          const card = createVariantCard(variant, category);
          grid.appendChild(card);
        });
      }
    }
  }

  document.addEventListener('DOMContentLoaded', () => {
    const filterButtons = document.querySelectorAll('.filter-btn');
    
    // Render initial view (all products)
    renderProducts('all');

    // Add filter button handlers
    filterButtons.forEach(button => {
      button.addEventListener('click', () => {
        const category = button.getAttribute('data-category');

        // Update active button styling
        filterButtons.forEach(btn => {
          btn.classList.remove('active', 'bg-blue-600', 'text-white', 'shadow-md');
          btn.classList.add('border', 'border-blue-600', 'text-blue-600');
        });
        button.classList.add('active', 'bg-blue-600', 'text-white', 'shadow-md');
        button.classList.remove('border', 'border-blue-600', 'text-blue-600');

        // Render products for selected category
        renderProducts(category);
      });
    });
  });

  document.addEventListener('visibilitychange', () => {
    if (document.hidden) {
      stopAllCarousels();
    } else {
      const activeCategory = document.querySelector('.filter-btn.active')?.getAttribute('data-category');
      if (activeCategory === 'all') {
        Object.keys(productsData).forEach(cat => {
          startCarousel(`carousel-${cat}`);
        });
      }
    }
  });
</script>
